Purpose: Global and shared style files.

The `legacy/` folder contains CSS moved from the previous static implementation and should remain untouched unless the legacy pages are being converted deliberately.
