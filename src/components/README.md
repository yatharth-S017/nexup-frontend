Purpose: Reusable React building blocks shared across pages.

Keep generic primitives in `ui/`, page framing in `layout/`, inputs in `forms/`, repeated display surfaces in `cards/`, app navigation in `navigation/`, user feedback states in `feedback/`, and compatibility wrappers in `common/`.
