export default function PlaceholderPage({ title }) {
  return (
    <main style={{ minHeight: '100vh', padding: '48px', fontFamily: 'DM Sans, sans-serif' }}>
      <h1>{title}</h1>
    </main>
  );
}
