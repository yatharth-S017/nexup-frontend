Purpose: Static source assets that are imported by React code.

Use `images/`, `icons/`, and `fonts/` for app-owned assets. Public URL assets that legacy HTML still references can stay in `Public/` until those pages are fully converted to React.
